{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the Sift main window: IPC invoke, scan event stream, native folder picker, and reveal-in-Explorer.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:default",
    "shell:allow-open"
  ]
}
