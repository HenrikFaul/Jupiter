package com.jupiter.filemanager.domain.model

/**
 * High-level classification of a file or directory used throughout the app for
 * iconography, filtering, and storage categorization.
 */
enum class FileType {
    FOLDER,
    IMAGE,
    VIDEO,
    AUDIO,
    DOCUMENT,
    PDF,
    ARCHIVE,
    APK,
    CODE,
    OTHER,
}
