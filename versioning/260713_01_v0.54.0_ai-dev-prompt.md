# AI development record — Jupiter v0.54.0

Date: 2026-07-13

Change package: `260713_01`

Repository/branch contract: `HenrikFaul/Jupiter`, direct synchronized `main`

## Objective

Repair the existing Android file manager's remaining device-trust gaps without rebuilding the app or regressing any delivered capability: retail storage capacity, understandable/safe Automation, complete duplicate totals and controls, and dependable downloaded-image arrival alerts.

## Authoritative user requirement

The user supplied four device screenshots and required:

- Jupiter must show/analyze the same full device capacity as the OEM storage UI;
- Automation needs at least five useful default choices, all suspended, plus list/edit/rename/delete-rule/activate/suspend/apply/safe-test behavior and a detailed step-by-step guide; file deletion inside Automation is forbidden;
- the duplicate headline must count image/similar results too;
- duplicate size filtering and Select all/Deselect all must be restored, never selecting the keeper;
- downloading an existing image must finally trigger the duplicate alert;
- “FUNKCIONÁLIS REGRESSZIÓT NEM CSINÁLHATSZ”.

## Implementation constraints

1. Existing app only; no greenfield replacement.
2. Pull `origin/main` with `--ff-only` before work; work and deliver directly on `main`.
3. Preserve public names/signatures/order; additive parameters/state only.
4. Preserve full-hash exact proof, perceptual/structural review boundaries, keeper protection and Recycle Bin delete flow.
5. Never add an Automation file-delete operation. Rule removal is allowed but must clearly remove only the rule.
6. Use real device state; do not fake a 256 GB tier when Android supplies an authoritative physical-media value.
7. Protect existing DataStore rules during preset migration and prevent deleted presets from respawning.
8. Changelog is append-only; create summary + AI development record; read/update the external lessons orchestrator/bible.
9. Local Gradle, emulator runtime and GitHub Actions are separate evidence sources; claim only what actually ran.
10. Preserve the pre-existing untracked `versioning.zip`.

## Files inspected before editing

- `README.md`, full `changelog.md`, `versioning/VERSIONING_CONTROL.md` and chronological version records through v0.53;
- `docs/RUNTIME_DIAGNOSIS.md`, `docs/BUG_HUNT_REPORT.md`, `docs/DUPLICATE_HANDLER_PORT_ANALYSIS.md`;
- external `lessons_orchestrator.md` plus PART 1 / PLAT_ANDROID lessons;
- FORGE intake/product, data model, UX flow, UI/UX, API contract, art direction and data-platform system prompts;
- storage provider/models and all capacity call sites;
- Automation repository/model/engine/worker/screens/ViewModels;
- MediaStore source, observer, scheduler, reconciler, Room state and duplicate detector;
- Duplicates UiState/ViewModel/selection policy/screen/tests;
- the four supplied screenshots.

## Execution decomposition

1. Synchronize and audit the default branch/worktree.
2. Parallel non-overlapping implementation owners:
   - storage capacity/source/formatter/UI/tests;
   - Automation lifecycle/safety/guide/preview/tests;
   - duplicate UI and arrival delta/tests.
3. Integrator review of shared invariants and explicit conflict coordination for `CleanupScreen.kt`.
4. Diagnose and discard a parallel-agent-corrupted KSP cache; rerun a clean cache-disabled generation/test gate.
5. Run the full assemble + all unit tests + lint gate serially.
6. Install the produced APK on the available API 34 emulator and verify real UI/state.
7. Create a real byte-identical MediaStore arrival and inspect the notification service output.
8. Update version/build presentation, README, changelog, versioning and lessons.
9. Stage only scoped files (never `versioning.zip`), commit, push `main`, wait for concrete CI conclusion and synchronize evidence.

## Files expected to change/add

The authoritative exhaustive path list is in the paired v0.54 summary. Expected layers:

- build/version and What's New;
- storage provider + formatter + four capacity surfaces;
- Automation domain/repository/engine/screens/ViewModels + two test files;
- MediaStore delta observer/scheduler/reconciler/source + tests;
- Duplicates UiState/ViewModel/screen + tests;
- README/changelog/versioning and external Android lessons.

## Verification plan

- Pure unit truth tables for retail capacity, decimal formatting, preset migration, Automation action safety, preview/gateway behavior, distinct duplicate totals, size order and generation arrival.
- Full `assembleDebug`, `testDebugUnitTest`, `lintDebug`.
- XML count must have zero failure/error/skip.
- Runtime install/cold launch; no FATAL exception.
- UI hierarchy must contain five suspended presets, guide, preview, duplicate aggregate/control labels and advertised total.
- Real MediaStore-scanned duplicate must produce a duplicate notification with notifications granted.
- Diff/static review must confirm no direct Automation deletion, keeper policy retained and public API additions are compatible.
- GitHub Actions success only after a concrete pushed SHA/run concludes.

## Completion rule

The round is complete only when all requested behaviors are implemented, local and emulator gates are recorded, versionName/versionCode + README/changelog + both versioning records + lessons are updated, scoped files are committed to synchronized `main`, the same branch is pushed to `origin`, and the published CI/release result is checked without overstating platform limits.

## Evidence status before delivery

- Local targeted clean gate: success.
- Full local gate: success, 58 suites / 344 tests / zero failure/error/skip.
- API 34 emulator: install/cold launch/UI/safe preview/real arrival notification success.
- Remote CI/release: pending direct-main push at the time this record was created.
