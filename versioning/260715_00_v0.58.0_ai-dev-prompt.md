# AI development record — Jupiscan v0.58.0 duplicate precision repair

## Objective

Repair severe duplicate false positives without regressing exact detection, arrival alerts, indexing, selection safety, file operations, branding, or the review UI.

## Authoritative user requirement

The user reported that Jupiscan placed many “abszolút össze nem illő” images/videos into one package and requested a deep audit of file metadata creation, file-type-specific comparison, and grouping, explicitly authorizing learning from `C:\Work\ultimate-duplicate-handler` and forbidding functional regression.

## Implementation constraints

- Work directly on synchronized `main`; preserve unrelated local files.
- Keep exact identity tied to full file content hash.
- Near/similar evidence must be type-aware, explainable, review-only and fail closed.
- Do not let graph transitivity replace pairwise evidence.
- Preserve current index data with explicit Room migration; incompatible derived media descriptors may be narrowly requeued.
- Preserve Retry vs terminal UNHASHABLE semantics.
- Preserve Similar through cache/process restart.
- Update version, changelog, versioning and the external lessons bible.
- Treat local Gradle, device proof and GitHub Actions as separate evidence sources.

## Files inspected before editing

- `changelog.md`, `README.md`, chronological relevant records under `versioning/`, `VERSIONING_CONTROL.md`.
- `lessons_orchestrator.md` and the Android section of the external lessons bible.
- Jupiscan descriptor sources, Room schema/DAO/repository, detector/backfills, cache, UI and regression tests.
- UDH `src-tauri/src/engine/videohash.rs`, `imagehash.rs`, DB migrations and grouping references.

## Execution steps

1. Pull `origin/main` with fast-forward only and inspect dirty state.
2. Trace screenshot-visible media rows through descriptor creation, DAO persistence, arrival lookup, batch grouping, UI and disk cache.
3. Prove the single-frame + Hamming-10 + union-find bridge root cause.
4. Introduce versioned ordered media descriptors and per-type hard gates.
5. Replace visible single-link results with bounded complete-link confirmation.
6. Require independent stacked evidence for images.
7. Add an in-place migration that requeues only incompatible media descriptors.
8. Version the disk snapshot and preserve `similar`; reject unsafe legacy cache.
9. Correct UI terminology so Similar never presents as exact.
10. Add positive and negative regression tests, including explicit A~B~C bridge cases.
11. Run compile, targeted tests, full tests, lint and APK build; record independent evidence.
12. Update docs, external lesson, commit and push `main`; inspect remote CI without inventing its result.

## Expected changed/added files

The implementation is limited to the index/dedup data layer, Room migration, duplicate cache/UI copy, tests, build version, README/changelog/versioning and external lesson. No navigation route, package ID, signing identity, file-operation gateway, Vault, remote transport or automation execution contract is removed.

## Verification

- Matcher tests: multi-frame positive, shared-one-frame negative, black-frame negative, duration/page-count veto, legacy fail-closed.
- Repository tests: image/video transitive bridge cannot form heterogeneous group.
- Detector tests: recompressed same image still alerts; different image remains unique; media arrival wiring stays live.
- Cache tests: cold-reload Similar preservation and v1 rejection.
- Full unit suite, debug compile, lint, APK assembly and SHA-256.
- Device result is only claimed if a real attached device run occurs.

## Completion rule

Not complete until version 0.58.0/code 10, changelog, both versioning records, lesson taxonomy, full tests, lint, APK/hash, direct-main commit/push and concrete remote status are recorded; all unrelated working-tree files remain untouched.
