# v0.16.0 — Verification research round 2 (factors #101–200)

**Date:** 2026-07-02
**Change package:** 260702_21
**Version:** 0.16.0
**Scope:** Jupiter Android app development — research/verification artifacts.

## Goal / why

Complete the 20-discipline research the user commissioned. Round 1 delivered 100 factors for 10
disciplines before provider session limits; this round completes the other 10.

## Added

- **`docs/RESEARCH_VERIFICATION_FACTORS_ROUND2.md`** — **100 further cross-method verification factors**
  (#101–200), top 10 per discipline across: Competitor-UX teardown (Files by Google / Solid Explorer /
  MiXplorer / Total Commander / CX / ZArchiver benchmarking), Archive/compression correctness (zip-slip,
  zip-bomb, encoding, zip64, symlink entries), Media/EXIF handling (orientation, HEIC/AVIF, video frame,
  color-space), i18n/RTL (mirroring, locale collation, formatting, Unicode normalization), QA methodology
  (Robolectric/instrumented/property/fuzz/screenshot/mutation), Battery/background (WorkManager
  constraints, Doze, foreground-service rules), Visual design/motion (M3 tokens, dynamic color, contrast),
  Trust/onboarding/permissions UX (rationale, deceptive-pattern avoidance), Product/business metrics
  (activation/retention/funnel, no dark patterns), Observability/errors (crash-free rate, ANR, structured
  error taxonomy, no-PII log hygiene). Each factor re-verifies an existing capability with a different
  method + standard + pass criterion + automation; **631-source appendix**.

## Modified

- **`docs/RESEARCH_100_VERIFICATION_FACTORS.md`** — summary now links the companion doc; total is
  **200 verification factors across all 20 disciplines (~1,260 sources)**.
- `app/build.gradle.kts` — `versionName` → 0.16.0.

## Acceptance criteria (met)

- All 20 disciplines covered; 200 total factors; each re-verifies an existing feature (not a new
  feature) with an independent method + citeable source. ✔

## Regression checks

- Docs-only change (plus version bump); no code touched. No capability affected.

## Verification

- Workflow `wpirybxny` — 10/10 disciplines, 631 sources, 170 candidate factors; salvaged from the run
  journal and rendered to the doc in the main loop (top 10 per discipline).
- CI unaffected (docs + version bump).

## Remaining risk / rollback

- None (documentation). Rollback = revert this commit.
- Planned next: convert more high-priority factors into enforced CI tests (path self-overwrite guard,
  MediaQuality ordering, locale-aware sort, formatter byte-units), and act on the competitor-UX findings
  (empty/error-state audit per NN/g, action-placement) where they reveal a real UX improvement.

## Lessons captured this iteration

- Splitting a large web-research sweep into a **second workflow after the provider limit reset** recovered
  the full 10 disciplines that the first run lost — pace heavy web fan-outs to the provider's session
  window. (Sync to `…/lessons-system/lessons_orchestrator.md`.)
